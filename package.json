{
  "name": "hjlees-com-websocket",
  "version": "1.0.0",
  "description": "WebSocket-enabled Flask app for hjlees.com",
  "scripts": {
    "start": "gunicorn --worker-class gevent -w 1 --bind 0.0.0.0:$PORT start_simple:app"
  },
  "engines": {
    "python": "3.12.0"
  }
}
